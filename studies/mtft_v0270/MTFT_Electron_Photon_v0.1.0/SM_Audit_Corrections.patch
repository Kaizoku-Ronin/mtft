--- a/src/mtft/constants.py
+++ b/src/mtft/constants.py
@@ -151,7 +151,17 @@
 
     @property
     def lambda_quartic(self) -> float:
-        """λ = (γ/Ω)²/2 (0.074%)"""
+        """λ = (γ/Ω)²/8, with V = −μ² H†H + λ(H†H)².
+
+        SM-AUDIT-01 (2026-09-07): enforce m_H² = 2 λ v², matching
+        HosotaniMTFT.higgs_self_coupling at the shared default VEV.
+        The superseded factor-four value remains available below.
+        """
+        return (EULER_GAMMA / LAMBERT_OMEGA)**2 / 8.0
+
+    @property
+    def lambda_quartic_RETIRED_SM_AUDIT_01(self) -> float:
+        """Historical 0.26.0 value; four times the canonical quartic."""
         return (EULER_GAMMA / LAMBERT_OMEGA)**2 / 2.0
 
 HIGGS = _MTFTHiggs()
--- a/src/mtft/lattice.py
+++ b/src/mtft/lattice.py
@@ -39,18 +39,39 @@
 #  SU(N) group elements
 # ═══════════════════════════════════════════════════════════════
 
-def random_su_n(N: int, rng: np.random.Generator | None = None) -> np.ndarray:
-    """Sample a random SU(N) matrix via QR decomposition of Gaussian."""
+def _random_unitary_qr(N: int, rng: np.random.Generator | None) -> np.ndarray:
+    """Shared QR draw, preserving the historical random-number sequence."""
     if rng is None:
         rng = np.random.default_rng()
     Z = (rng.standard_normal((N, N)) + 1j * rng.standard_normal((N, N))) / math.sqrt(2)
     Q, R = np.linalg.qr(Z)
-    # Fix phases to make det = 1
+    # Fix QR phases; determinant normalization is a separate operation.
     d = np.diag(R)
     ph = d / np.abs(d)
     Q = Q @ np.diag(ph.conj())
-    det = np.linalg.det(Q)
-    Q[0, :] /= det ** (1.0 / N)
+    return Q
+
+
+def random_su_n(N: int, rng: np.random.Generator | None = None) -> np.ndarray:
+    """Sample a random SU(N) matrix via QR decomposition of Gaussian.
+
+    SM-AUDIT-02 (2026-09-07): multiplying one row changes the determinant
+    by that full factor, so divide that row by det(Q), not its Nth root.
+    """
+    Q = _random_unitary_qr(N, rng)
+    Q[0, :] /= np.linalg.det(Q)
+    return Q
+
+
+def random_su_n_RETIRED_SM_AUDIT_02(
+    N: int, rng: np.random.Generator | None = None
+) -> np.ndarray:
+    """Historical 0.26.0 draw: unitary but generally NOT special unitary.
+
+    Retained only to reproduce the defect; never used by hot_start.
+    """
+    Q = _random_unitary_qr(N, rng)
+    Q[0, :] /= np.linalg.det(Q) ** (1.0 / N)
     return Q
 
 
--- /dev/null
+++ b/tests/test_sm_audit_corrections.py
@@ -0,0 +1,42 @@
+"""Public-contract regressions for SM-AUDIT-01 and SM-AUDIT-02."""
+import numpy as np
+import pytest
+
+from mtft.constants import HIGGS
+from mtft.hosotani import HosotaniMTFT
+from mtft.lattice import (
+    LatticeConfig, random_su_n, random_su_n_RETIRED_SM_AUDIT_02,
+)
+
+
+def test_public_quartic_reproduces_higgs_mass_and_hosotani():
+    assert 2 * HIGGS.lambda_quartic * HIGGS.V_EW**2 == pytest.approx(
+        HIGGS.m_H**2, rel=2e-14
+    )
+    assert HIGGS.lambda_quartic == pytest.approx(
+        HosotaniMTFT().higgs_self_coupling(), rel=2e-14
+    )
+    assert HIGGS.lambda_quartic_RETIRED_SM_AUDIT_01 == pytest.approx(
+        4 * HIGGS.lambda_quartic, rel=2e-14
+    )
+
+
+@pytest.mark.parametrize('N', [2, 3, 5])
+def test_random_su_n_group_membership(N):
+    for seed in range(16):
+        U = random_su_n(N, np.random.default_rng(seed))
+        np.testing.assert_allclose(U.conj().T @ U, np.eye(N), atol=4e-14, rtol=0)
+        assert abs(np.linalg.det(U) - 1) < 4e-14
+
+
+def test_hot_start_uses_corrected_sampler():
+    cfg = LatticeConfig(N=3, L=2)
+    cfg.hot_start(np.random.default_rng(20260907))
+    links = cfg.links.reshape(-1, 3, 3)
+    assert np.max(np.abs(np.linalg.det(links) - 1)) < 4e-14
+    assert np.max(np.abs(links.conj().transpose(0, 2, 1) @ links - np.eye(3))) < 4e-14
+
+
+def test_historical_sampler_reproduces_recorded_defect():
+    U = random_su_n_RETIRED_SM_AUDIT_02(3, np.random.default_rng(0))
+    assert abs(np.linalg.det(U) - 1) == pytest.approx(1.0278965, abs=1e-7)
--- /dev/null
+++ b/SM_AUDIT_CORRECTIONS_2026-09-07.md
@@ -0,0 +1,21 @@
+# Local audit corrections to MTFT 0.26.0
+
+This is a local audit variant dated 7 September 2026, not a newly published MTFT release. The upstream package version remains 0.26.0. The original uploaded release is preserved separately.
+
+## SM-AUDIT-01 — Higgs quartic convention
+
+The public `HIGGS.lambda_quartic` previously returned `(gamma / Omega)**2 / 2`. With the package's `m_H = v * gamma / (2*Omega)`, the canonical potential `V = -mu² H†H + lambda (H†H)²` requires `lambda = m_H²/(2v²) = (gamma/Omega)²/8`. The public value is corrected from 0.5179175466708401 to 0.12947938666771003. This agrees with `HosotaniMTFT.higgs_self_coupling()` at the shared default VEV. The old value remains under `HIGGS.lambda_quartic_RETIRED_SM_AUDIT_01`.
+
+This change resolves the factor-four inconsistency. It does not resolve the separately recorded local/global VEV behavior or the distinct W/Z ratio formulae.
+
+## SM-AUDIT-02 — Random SU(N) initialization
+
+The QR initializer divided one row by the Nth root of the determinant. A factor applied to only one row multiplies the determinant by that factor, so the correction must use the entire determinant. `random_su_n` now divides the first row by `det(Q)`. The QR draw and random-number sequence are preserved in a shared helper. Historical invalid special-unitary draws remain reproducible through `random_su_n_RETIRED_SM_AUDIT_02`.
+
+The corrected sampler is used automatically by `LatticeConfig.hot_start`. Previous hot-start trajectories generated by the defective sampler require rerunning before they can be treated as SU(N) simulations. This note does not certify the distribution through statistical tests or establish any lattice continuum result.
+
+## Validation scope
+
+Ten targeted pytest cases passed: six in `tests/test_sm_audit_corrections.py` and four existing Higgs-sector cases. Additional companion experiments checked 48 draws across N=2,3,5 and invariance of both implemented action terms under a local SU(3) transformation on one periodic 2^4 lattice. The full package test suite was not run.
+
+Audit identifiers are retained from the companion Standard Model assessment; no new numbered CC entry is assigned here.
