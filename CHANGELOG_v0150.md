# v0.15.0 — the certification wave (2026-08-12)

One new module (weil), one replaced module (curvature), 20 new tests,
a 29-file study bundle, five new corpus corrections (CC-02…CC-06), one
renumbering (du03 CC-02 → CC-07), and the adjudication of the v6 wave's
single outstanding FAIL (C9b) — E2 certified by two routes sharing no
steps.

## New module: weil

* `weil.py` — the compressed Weil form: a fourth column for the Three
  Ensembles program. Kernel Φ(γ−γ′)² band-limited to |α| ≤ λ on zero
  pairs; localization = the bandwidth window; criterion = inertia of the
  compressed form. Provides `Window`, `gabor`, `nu_parts`, `G_prime`,
  `G_zero` (synthetic depths and multiplicities), `certificates`,
  `rank_trace_gap` / `audit_rank_trace`, `mt_constant`,
  `w_series_check`, `selftest`. Sympy-free; scipy optional.
* Certified by the W1 bundle (studies/v0150_2026aug): Lemma 3.2 re-proved
  independently by the auditor; W1-P1 E2 max relative discrepancy
  3.472e-09 (pre-registered ≤ 1e-5); W1-P2 C/N = 0.73398 inside the
  pre-registered [0.729, 0.739] (external anchor 0.734).
* `tests/test_weil.py` (7 tests); the E2 test consumes
  `tests/zeros_gamma_T100.npy` — 371 ordinates, max γ = 640.6947946688257,
  regenerated by the auditor from the certified sweep (3994 zeros to
  γ = 4500.16), matching the study's stated parameters exactly.

## Replaced module: curvature

* `curvature.py` replaces the v0.14.0 file: the rigidity theorem (a
  2-dim discrete exponential family supported on a line plus exactly one
  off-line point has K = 1/4 identically — {1,2,3,4} is in the class
  because w_4 = (1/2) log 4 gives X_4 = 2 X_2 exactly), out-of-sample
  confirmations on {1,2,4,8} / {1,2,4,16} to better than 1e-115,
  sharpness gated in both directions ({1,2,4,8,16} → −11,
  {1,2,3,4,5} → 476), `rigidity_class(points)` helper, the
  det g = weighted squared-triangle-area identity cross-checked against
  the independent Brioschi route.
* **CC-04**: `cold_amplitude()` is now the closed form
  c = (9 L5²)/(25 L2 L3) [1 − (9/5) L5/L3 + (4/5) L5/L2]
  = 0.27012646530542495706433719670365…; the six-atom β=200 extraction
  (0.270126465305424759517602, wrong in the 16th digit by atom-6
  contamination (5/6)^200 = 1.46e-16) is preserved as
  `cold_amplitude_RETRACTED_CC04`.
* **CC-03**: `finite_atom_curvature` precision is adaptive (doubling,
  rtol 1e-20); a caller `dps` may only raise the floor. The old fixed
  floor returned −2e+19 instead of 1/4 on {1,2,4,8} at β=80 — silently.
* `tests/test_area_geometry.py` (13 tests).

## C9b adjudication — E2 CERTIFIED

The v6 wave's 19 PASS / 1 FAIL is now 19 PASS / 1 ADJUDICATED. The
[∞, 2/77] f1-period sign dispute (archive PARI 2.17 λ₁ = +½+1.0232…i vs
v5/v6 PARI 2.15.4 λ₁ = −½−1.0232…i) resolved by two routes sharing no
steps:

* Route A (exact rational Manin symbols): Re λ₁ = −1/2 exactly ⟹ v6.
* Route B (q-expansion slash-integrals, own 60000-term aₙ sieve,
  Gauss-Legendre panels, no PARI `mfsymboleval`): per₂₇₇ reproduces v6
  to ~1e-7; per₁₁ matches both engines exactly; {1/38, 2/77} = −per₁₁
  to 4e-15 by Manin integrality.

**Verdict: v6 sign CORRECT; the Paper 33 v2 archive needs the sign-only
correction** (per the v6 report's pre-registration). Evidence:
studies/v0150_2026aug/c9b_exact_symbol.{py,json}, c9b_routeB.json.

## Corpus corrections filed (append-only)

* **CC-02** — Σ wₙ n⁻ˢ = F(s+1) = −ζ(s)·ζ′(s+1). Paper 1 Prop 1.5's
  −ζ′(s+1) is the *summand* series; AG Pr 4.1.4 mistranscribes.
  Three-route E2 (sieve 7.1e-15; s=3 numeric 5.2e-12; F-consistency).
  Downstream exposure nil.
* **CC-03** — adaptive precision (above).
* **CC-04** — cold amplitude closed form (above).
* **CC-05** — MTFT_Chapter5 Table 1: w₆ = 1.011404 = (4log2+3log3)/6,
  w₈ = 0.953077 = 11log2/8, w₉ = 0.610340 = 5log3/9,
  w₁₀ = 0.898720 = (6log2+3log5)/10. Two routes to 1e-12.
* **CC-06** (proposal) — M8 study mixed-basis bug: true
  dim commutant({Hecke, V}) = 2 = span{I, Z} (study printed 1);
  canonical M8b amplitude 0.20610964892935077 (not 0.3805). Auditor
  reproduction on six primes: commutant(Hecke) = 60, commutant(V) = 82,
  full Hecke algebra = 52.
* **CC-07** — the du03 metric correction, renumbered from CC-02 on
  2026-08-12 to resolve the collision with the w-series CC-02; content
  unchanged, note prepended to studies/hodge_2026aug/CC-07_du03_metric_correction.md.

## Study bundle

studies/v0150_2026aug/ — 29 files: wave artifacts byte-preserved (V6
period matrix + GP transcript + period data, W1 study + driver, area
geometry + ledger, M8/M8b/M9 + ledgers, INTEGRATION note, weights fact
ledger), auditor artifacts (CC files, m8_verify/deep reproductions, C9b
E2 evidence). The README carries the byte-preservation policy, the
internal→repo CC label mapping, the A8 historical-demonstration-gate
disclosure, and the M9/R7-open status.
