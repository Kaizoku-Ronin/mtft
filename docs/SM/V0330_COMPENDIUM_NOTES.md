# v0.33.0 — exact inputs and qualifications from the theorem compendium, applied to the toolset (2026-09-23)

The compendium (Parts A–E, a teaching document kept outside the package) re-proved the frozen propositions I.1–VI.5.  Besides CC-33
(its own register) it produced the following exact inputs and qualifications.  Each names its package function and test.

## 1. X0(143) is neither hyperelliptic nor trigonal (compendium Lemma E.1) — EXACT, two routes
#X0(143)(F_2) = 4 and #X0(143)(F_4) = 18 from the certified T_2 (charpoly x^2 (x+2)^4 g^2 s^2: tr T_2 = -1 and tr T_2^2 = 39 on S_2;
Eichler–Shimura).  Second route: at least 14 supersingular points over F_4 (Eichler mass (p-1) psi(143)/24 = 7, every weight <= 1/2) plus
the four rational cusps.  A g^1_e with e <= 3 is unique for genus >= 5, hence defined over Q, and its reduction mod 2 would force
#X(F_4) <= 15.  Consequence: h^0(O(D)) = 1 for every effective D of degree <= 3 — h^0(O(P+Q)) = 1 and h^0(O(P1+P2+P3)) = 1 are now
proved.  Used by: the degree-3 Landau cluster (KKT01), M3's massive multiplicity 10 (R2C-05), the S2 Higgs count (R2C-09), the rank
analysis below.  Second route: the canonical ideal of X is generated by quadrics (`canonical.gates.gate_generation`, P7 = P8 = 395), which by Petri
excludes a g^1_3 as well.  `hecke.point_counts_from_T2`, `hecke.supersingular_lower_bound`, `hecke.gonality_lower_bound`; `tests/test_v0330.py`.

## 1b. h^0(O(2 sum P)) = 1 — EXACT (closed the same day; `canonical.gates.gate_petri_w13_quotient`)
Let Y = X0(143)/W13 (genus 6) and Q_i the images of the three W13-fixed CM points P_i.  Invariant differentials vanish at fixed points
(the local action is z -> -z) and descend to Y, anti-invariant ones have vanishing derivative there; so H^0(K_X(-2 sum P)) =
H^0(K_Y(-sum Q)) (+) {anti-invariant differentials vanishing at sum P}, the second summand of dimension 4 by item 1.  Riemann–Roch on both
curves then gives h^0(O_X(2 sum P)) = h^0(O_Y(Q1 + Q2 + Q3)) (equivalently pi^* O_Y(sum Q) = O_X(2 sum P)).  The canonical curve of Y in P^5
is the projection of X's to the six W13-invariant adapted coordinates (sectors (+,+) and (-,+)); its quadrics are the quadrics of I_2(X)
supported on those coordinates: exactly 6 = (g-2)(g-3)/2 (a hyperelliptic Y would satisfy 10).  Their cubic products span 31 = 56 - 25
dimensions and their quartic products 91 = 126 - 35 (over Q and mod two primes), i.e. I_3(Y) = S_1 I_2(Y): by Petri, Y is neither trigonal
nor a plane quintic.  Hence gonality(Y) >= 4, h^0(O_Y(sum Q)) = 1, and h^0(O_X(2 sum P)) = 1.  M1's massive multiplicity 7 = 6 + 1 (R2C-05)
and the degree-6 entry of `product_surface.curve_cohomology` are now exact.  No CM-point coordinates are needed.

## 2. The vector-Higgs mode operator in Kodaira form (compendium V.2–V.3) — EXACT
For a block with its HYM metric, on ANY Kaehler metric of the curve: tachyonic polarisation m^2 = 2 d*d - |B| (the (0,1)-forms of the
negative-degree member, sections of L_neg (x) K^-1), massive polarisation m^2 = 2 dbar*dbar + |B| (sections of L_neg (x) K); the Gaussian
curvature cancels pointwise (the Ricci term against the curvature of Lambda^{0,1} = K^-1), not only on average as R2C-02 stated.  The
lowest tachyonic level -2 pi |d|/A has multiplicity h^1(L_neg) = |d| + g - 1 — Atiyah–Bott's Morse index of the split connection; the
sign of the magnetic-moment term is fixed by that invariant statement and confirmed by the closed-form Nielsen–Olesen (flat torus) and
Brandt–Neri–Coleman (sphere) spectra.  R2C-02's (0,1)-formula is restricted to d < 0 (this is CC-30 made part of the statement).
`vector_higgs.kodaira_forms`, `vector_higgs.constant_curvature_checks`.

## 3. The Higgs level on the surface (compendium VI.3, remark) — EXACT
With the product connection and product metric the lowest Higgs level of S1 and S2 is 2 pi mu(L_H)/(A_X A_E): the (0,1)-leg factor
contributes -2 pi |deg|/A (item 2) and the other factor its scalar Landau level.  Massless exactly on the slope-free locus, tachyonic for
mu < 0, massive for mu > 0 — R2C-07's "electroweak scale = Kaehler deviation" with a proof.  `product_surface.higgs_slope_mass`.

## 4. VI.3 conventions made explicit (R2C-09)
A family's bidegree is that of its net chiral zero modes (the degree-1 curve types have both kinds: (1,-3) has (3, 6)); the degree-1 curve
factor is S0(P) with P in {P1, P2, P3} — for P = P4 the S2 Higgs factor O(sum P - P4) has no sections; torus x torus pairs fail like
curve x curve pairs; the "mirrors" exchange Q and u^c; conjugate pairs are never admissible.  `product_surface.surface_solutions` docstring.

## 5. The up-mass rank bound, qualified (R2C-09 structural consequence; compendium VI.4)
M_{i,(a,j)} = sum_{r,s} v_{rs} A_{iar} B_{js} with the u^c curve factor H^0(S0(P)) two-dimensional gives rank M <= min(3, 2 * kunneth_rank(v)).
S2: the curve Higgs factor H^0(O(sum P - P)) is one-dimensional, so every VEV has Kuenneth rank one and rank M <= 2 unconditionally
(m_u = 0 at leading order; A is the Serre pairing of H^1(S0(-sum P)) with the 2-dimensional image of H^0(S0(P)), of rank exactly 2).
S1: the curve Higgs factor is 16-dimensional and the torus factor 2-dimensional; the image of w -> A(w) is {A : A_12 = A_21} (the one
relation (su) s = s (su), using h^0(O(sum P)) = 1 of item 1), and a Kuenneth-rank-two VEV with A(w1) = E11, A(w2) = E22 + E31 gives
rank 3.  So "m_u = 0 at leading order in both solutions" holds for S2 and, in S1, for VEVs of Kuenneth rank one.  R2C-08's "rank M <=
rank v" is correct with rank meaning Kuenneth rank, times the 2 of the curve factor.  `theta_torus.up_mass_rank_bound` (qualified),
`theta_torus.up_mass_rank_examples`.

## 6. The torus factor on a second route (compendium VI.5) — CERTIFIED (numerical, two routes)
Exact: c4 = 64, c6 = 1864, Delta = -1859, j = -262144/1859; j < 0 puts tau on Re tau = 1/2 above rho.  Theorems (proved in the compendium):
the theta bases are orthogonal with ||theta_{k,j}||^2 = (Im tau/2k)^{1/2}; theta_{1,0} theta_{k,b} = sum_j c_j(b) theta_{k+1,j} with
c_j(b) = sum_l exp(pi i tau k(k+1)(l + (j - b - b/k)/(k+1))^2).  Hence B_{j,b} = c_j(b) (2k/((k+1) Im tau))^{1/4} without quadrature;
it agrees with `torus_factor` entrywise to 1e-25, and tau from the q-series of E4^3/Delta agrees with kleinj to 1e-26.  tau is
transcendental (Schneider; 143a1 is not CM), so the singular values are a numerical certificate on two routes, not exact arithmetic.
`theta_torus.tau_143a1_qseries`, `theta_torus.theta_norm_closed_form`, `theta_torus.torus_factor_closed_form`.

## 7. Method note (D4 closure gate, `tests/test_v0230.py`)
The greedy absolute gate of `liealg.close_lie` normalised near-dependent residuals and sat inside its ambiguity window on one platform
(residual 1.25e-7 on Windows, 9.3e-8 on Linux) for reasons of the algorithm, not of the data.  `liealg.close_lie_svd` (singular-value
rank with a spectral-gap gate) reads the D4 closure as [3, 6, 17, 28, 28] with structure residual 8e-12 and gaps above 1e9 in every
round; `d4_report` uses it.  A method change, not a change of result: the CERT(tol, E2) fingerprint stands.  Also applied: the
`computational_stiffness` timing assertion (perf_counter; finite and non-negative).

## Status of the frozen propositions after the compendium (32)
As frozen 15; tier or wording 5; hypothesis or scope made precise 8; strengthened 2; corrected 2 (IV.3 = CC-33; VI.4 for S1, item 5).
Closed the same day: h^0(O(2 sum P)) = 1 (item 1b).  Open: 8D anomalies on the surface; Kaehler-moduli stabilisation at A_X = 2 A_E or
A_E/2; the three vector-like u^c pairs; the down and lepton triangles on the surface.
