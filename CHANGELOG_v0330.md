# mtft v0.33.0 — the theorem compendium applied to the toolset; CC-33 (2026-09-23)

**CC-33** (`docs/SM/CC33_TENSOR_COEFFICIENT.md`): the six-dimensional self-dual tensor's p2 coefficient is -7/360 (28 Weyl units), not the
+1/360 of R2C-03 (its p1^2 coefficient).  p2 cancellation by chiral tensors needs n_grav = 0 mod 28: none of M1's ledger-preserving
assignments survives, and the pure-gaugino rule dim(adj) = 0 mod 28 fails for E6, E7, E8.  `research.gravitational_anomaly`
(`P2_SELF_DUAL_TENSOR`, `P1SQ_SELF_DUAL_TENSOR`, `tensor_coefficient_witnesses` — the genera, 273 = 245 + 28, 1/48 = 30/1440),
`unified_parent.gaugino_p2_integrality`, gate `six_d_anomaly_lift` (full-table witness; verdict unchanged).  Tests re-derive the constant
instead of re-asserting it.

**Exact inputs from the compendium** (`docs/SM/V0330_COMPENDIUM_NOTES.md`):
- `hecke.point_counts_from_T2` — #X0(143)(F_2) = 4, #X0(143)(F_4) = 18 from the certified T_2 (Eichler–Shimura), matched by
  `hecke.supersingular_lower_bound` (14 + 4 cusps); `hecke.gonality_lower_bound` — X0(143) is neither hyperelliptic nor trigonal, so
  h^0(O(D)) = 1 for every effective divisor of degree <= 3 (h^0(O(sum P)) = 1 proved).
- `canonical.gates.gate_petri_w13_quotient`, `w13_quotient_quadrics` — the genus-6 quotient X0(143)/W13 has its canonical ideal generated by
  its 6 quadrics (ranks 31 and 91), so it has gonality >= 4 by Petri; with h^0(O_X(2 sum P)) = h^0(O_Y(Q1+Q2+Q3)) this proves
  h^0(O(2 sum P)) = 1 — M1's massive multiplicity 7 = 6 + 1 is exact and `curve_cohomology`'s degree-6 entry is no longer a register value.
  `canonical.gates.verify()` now includes this gate.
- `vector_higgs.kodaira_forms`, `constant_curvature_checks` — both polarisations as pointwise identities for any metric (2 d*d - |B| and
  2 dbar*dbar + |B|); the R2C-02 formula is restricted to negative-degree blocks (CC-30); closed-form checks on sphere, torus, genus 13.
- `product_surface.higgs_slope_mass` — the lowest Higgs level on X0(143) x 143a1 is 2 pi mu(L_H)/(A_X A_E) (R2C-07's claim, now proved);
  VI.3 conventions recorded (P in {P1, P2, P3}; torus x torus also fails).
- `theta_torus.up_mass_rank_bound` qualified: rank M <= min(3, 2 * kunneth_rank(v)); unconditional in S2, Kuenneth-rank-one VEVs in S1
  (`up_mass_rank_examples` shows rank 3 for a rank-two VEV in S1).
- `theta_torus.torus_factor_closed_form`, `theta_norm_closed_form`, `tau_143a1_qseries` — the torus factor from the theta multiplication
  formula without quadrature (agreement 1e-25) and tau from the q-series of E4^3/Delta (1e-26): a second route for VI.5.

**D4 closure gate** (`liealg.close_lie_svd`, `spectral_rank`; `d4_report` uses it): singular-value rank with a spectral-gap gate replaces the
greedy absolute gate that sat inside its ambiguity window on Windows (method note; result unchanged).  `computational_stiffness` timing
assertion made platform-safe (perf_counter).

**Tests**: `tests/test_v0330.py` (CC-33 units and rule; point counts on two routes and gonality; Kodaira forms on three geometries; the surface
enumeration from scratch; rank examples; closed-form theta against the pinned values, and against quadrature under `slow`),
`tests/test_compendium_chapter_I.py` (Chapter I gates I.1, I.3b, I.3c, I.4, I.5, I.5B, exact arithmetic only), updated
`test_route2_r2c03/04/09.py`, `test_v0230.py`, `test_arithmetic_machine.py`.  Registers: CC-33; addenda to R2C-02/03/05/07/09 and CC-29
(append-only).  Legend: 8 entries (`cc33_tensor_coefficient`, `x0143_gonality_bound`, `w13_quotient_petri`, `vector_higgs_kodaira_form`, `surface_higgs_slope_level`,
`up_mass_rank_kunneth`, `theta_torus_closed_form`, `lie_closure_svd_gate`).  Base: 0.32.1.  Pin four-way.  Teaching materials (the compendium
Parts A–E) stay outside the package.
