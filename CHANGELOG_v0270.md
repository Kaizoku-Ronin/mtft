# mtft v0.27.0 — canonical-frame intertwiner, cross-frame Hodge E2, research studies tree (2026-09-08)

## `mtft.surface.intertwiner` (EXACT) — closes the Sp(26,Z) intertwiner item
Pi = L^T C, with L the canonical mslattice symbol basis (regenerated by PARI and equal to the
frozen `mtft.homology` W11/W13/STAR/P exactly) and C the Wave 10.1 cycle basis.  Frozen in
`surface/_data` and gated at load: Pi in GL(26,Z) (det -1), Pi W_cyc = W_L^T Pi for W11 and W13,
and Poincaré duality P_canonical = -Pi Jint^-1 Pi^T.  The cycle frame and the canonical homology
frame are now one lattice with an exact map (23 frozen gates).

## Cross-frame Hodge check (CERTIFIED, frame-independent)
The `mtft.periods` frame has no stored integer map, so the Hodge structures are compared by the
j-invariants of the two 2-dimensional AL sectors: (+,+) gives j(143a1) = -141.0134480904 in both
frames (3e-14); (-,-) gives j = -4096/11 in both frames (2e-14).  New exact fact: the (-,-)
Atkin-Lehner sector of H_1(X0(143), Z), with its polarization and Hodge structure, is the elliptic
curve 11a3 (5-isogenous to 11a1), not 11a1 — the isogenous-curve question left open in Wave 10.4.

## Research studies tree (repository only; pruned from the sdist)
`studies/mtft_v0270/` carries Astra's 15 frozen v0.26.x studies (extracted with hash checks),
the additive research tools patch, and Astra's release-notes draft.  Verified here: bundle
integrity (136 files, 15 archives), the exact dyadic short-horizon certificate replays (25 rows),
the Ising DOS reference equals `surface.ising.density_of_states` (85 integers), and the
compatibility decisions match CC-21/CC-22 and the existing `mtft.boundary` namespace.

## Housekeeping
CITATION.cff taken from the live 0.26.2; release pin bumped four-way.
Tests: `tests/test_surface_intertwiner.py` (2).
